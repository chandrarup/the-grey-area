import { StagePlaceholder } from "../stage-placeholder";

export default function DebriefPage() {
  return (
    <StagePlaceholder
      title="Debrief"
      body="Your per-decision feedback and overall summary will appear here once the grading layer is built."
      ctaLabel="Finish"
      ctaHref="/"
    />
  );
}
